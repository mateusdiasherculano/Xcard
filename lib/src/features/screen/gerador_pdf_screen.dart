import 'package:flutter/material.dart';

class GeradorPDF extends StatelessWidget {
  const GeradorPDF({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
